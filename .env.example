# Simplest local setup: paste this in the UI or export it for dev scripts.
GITHUB_TOKEN=github_pat_xxx

# Pi SDK credentials; exact names may change once SDK docs are wired in.
PI_API_KEY=
PI_BASE_URL=

# Optional local cache path.
APP_DATA_DIR=.data
